name: test_1

on: [push]    

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - 
        name: first step
        run: echo hello ${{ secrets.DOCKER_HUB_USERNAME }}
      - 
        name: second step
        run: echo done
