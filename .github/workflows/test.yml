name: first
on: [push]
jobs:
  a-job:
    runs-on: ubuntu-latest
    steps:
      - id: one
        name: step one
        run: echo 'step one'
      - id: two
        name: step two
      - run: echo 'step two'
      - run: echod 'step three'


